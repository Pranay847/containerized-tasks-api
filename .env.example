# Copy to .env and fill in. .env is gitignored; this example is committed.
POSTGRES_USER=tasks
POSTGRES_PASSWORD=change_me
POSTGRES_DB=tasks

# The app reads this. Host "db" is the compose service name.
DATABASE_URL=postgresql://tasks:change_me@db:5432/tasks
